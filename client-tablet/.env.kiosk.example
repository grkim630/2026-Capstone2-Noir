# Fully Kiosk / 전시장 태블릿용 — .env.kiosk.local 로 복사 후 IP 수정
#
# cp .env.kiosk.example .env.kiosk.local
# npm run dev -- --mode kiosk

# 서버 PC LAN IP (localhost 사용 금지)
VITE_SERVER_ORIGIN=http://192.168.0.10:8000

VITE_TABLET_SOCKET_ENABLED=true
VITE_TABLET_UI_ONLY=false
